from hydpy_mpr import Coefficient


coef_const = Coefficient(name="const", default=200.0)

coef_factor_sand = Coefficient(name="factor_sand", default=0.0)

coef_factor_clay = Coefficient(name="factor_clay", default=0.0)
